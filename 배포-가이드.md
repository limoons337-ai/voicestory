# 보이스토리 배포 가이드 (단일 서비스)

Express 백엔드가 **빌드된 프런트까지 함께 서빙**하도록 만들어서, **한 서비스·한 도메인**으로 배포됩니다. CORS·프록시·별도 프런트 호스팅 불필요. `/api`·assetlinks·아바타·SSE 스트리밍 전부 같은 오리진에서 동작. (로컬 검증 완료)

원스토어 앱은 이미 생성됨 — **Android AID: OA01008534, 패키지 `com.limoon.voicestory`**.

---

## 방법 A) Fly.io — git 없이 로컬에서 바로 배포 (추천)

```bash
# 1) flyctl 설치 (Windows PowerShell)
#    iwr https://fly.io/install.ps1 -useb | iex
fly auth signup     # 또는 fly auth login

cd C:\nameplate\voxrpg

# 2) 앱 생성 (fly.toml 이미 있음 — 그대로 사용). 배포는 아래서.
fly launch --no-deploy --copy-config --name voicestory --region nrt

# 3) Groq 키를 시크릿으로 (코드/설정에 안 남김)
fly secrets set LLM_API_KEY=gsk_당신의_키

# 4) 배포 (로컬 Dockerfile 빌드 → 배포)
fly deploy
```
→ `https://voicestory.fly.dev` 발급. 이게 **도메인**이자 TWA가 열 주소.

- 상시가동: `fly.toml`의 `min_machines_running = 1`, `auto_stop_machines = false` 로 (콜드스타트 제거).
- 재배포: `fly deploy` 다시.

## 방법 B) Render — GitHub 저장소 기반 (대안)
1. `git init && git add . && git commit` → GitHub에 push (`.env`는 .gitignore로 제외됨)
2. Render 대시보드 → New → Blueprint → 저장소 연결 (`render.yaml` 자동 인식)
3. 환경변수 `LLM_API_KEY`(시크릿, gsk_...) 입력 → 나머지는 render.yaml에 있음
4. 배포 → `https://voicestory.onrender.com` 발급 (free는 슬립 → starter 권장)

> 대안: Railway 도 같은 Dockerfile로 배포 가능.

---

## 배포 후 → TWA(AAB) → 원스토어

도메인(예: `https://voicestory.fly.dev`)을 얻은 뒤:

```bash
npm i -g @bubblewrap/cli
bubblewrap init --manifest https://voicestory.fly.dev/manifest.webmanifest
#  Application ID: com.limoon.voicestory  (원스토어 등록과 반드시 일치)
#  서명 키 생성 → 비밀번호·별칭 백업(분실 시 업데이트 불가)
bubblewrap build
#  → app-release-bundle.aab + 서명키 SHA256 지문
```

1. 출력된 **SHA256 지문**을 `web/public/.well-known/assetlinks.json`에 넣고 → **재배포**(`fly deploy`)
   - 확인: `https://voicestory.fly.dev/.well-known/assetlinks.json`
2. 원스토어 개발자센터(앱 OA01008534) → **AAB 업로드** + 아이콘/스크린샷/설명(→ `스토어-등록소재.md`, `store-screenshots/`) + 등급분류 → 심사 제출

---

## 필수 환경변수 (플랫폼에 설정)
| 키 | 값 |
|---|---|
| LLM_BASE_URL | https://api.groq.com/openai/v1 |
| LLM_MODEL | qwen/qwen3.6-27b |
| LLM_API_KEY | gsk_... (**시크릿**) |
| FREE_DAILY_TURNS | 30 |
| MAX_TOKENS | 320 |
| PORT | (Fly/Render가 자동, 코드가 process.env.PORT 사용) |

## 체크리스트
- [ ] Fly(또는 Render) 배포 → HTTPS 도메인 확보
- [ ] Groq 키 시크릿 설정 (채팅 노출본은 rotate 권장)
- [ ] 도메인 열어 앱 동작 확인 (`/api/health` 200)
- [ ] bubblewrap init/build → AAB + SHA256
- [ ] assetlinks.json 지문 넣고 재배포 → `/.well-known/assetlinks.json` 확인
- [ ] 원스토어 AAB 업로드 + 소재 + 등급분류 + 심사
