# 보이스토리 (VoiceStory) — 말로 하는 자유 시나리오 AI RPG (구독형)

> 한 줄 정의: **말만 하면 세아가 나만의 이야기를 실시간으로 만들어주는 음성 텍스트 RPG.**
> 앱 제목: **보이스토리 (VoiceStory)** · 캐릭터: **세아** (전 세계 공통 AI 이야기 동반자)
> (내부 폴더·패키지명은 `voxrpg` 유지)

---

## 1. 컨셉

- **자유 시나리오(free scenario)**: 정해진 대본 없음. 플레이어가 말한 행동을 AI GM이 받아 세계를 전개.
- **음성 우선(voice-first)**: 마이크로 말하면 → AI가 상황을 낭독. 손 안 대고 진행 가능.
- **세계 팩(World Pack)**: 무협/이세계/좀비 등 세계관을 골라 시작. 각 세계 = 시스템 프롬프트 1벌.
- **구독형**: 무료는 체험(세계 제한 + 하루 턴 제한), 유료는 전 세계 개방 + 넉넉한 턴.

## 2. 왜 이 조합인가 (기술 결정)

| 항목 | 선택 | 이유 |
|---|---|---|
| 플랫폼 | **PWA(웹앱)** | 브라우저 마이크 사용 가능(앱인토스 웹뷰는 마이크 불가). iOS/안드로이드 동시 커버, 심사 없음 |
| 음성 입력(STT) | 브라우저 Web Speech API | **비용 ₩0** |
| 음성 출력(TTS) | 브라우저 SpeechSynthesis | **비용 ₩0** (프리미엄 목소리는 후순위 옵션) |
| AI 두뇌(LLM) | **스위처블 백엔드** | 개발/베타는 Ollama(₩0), 유료 오픈 시 클라우드로 스위치 |

**핵심 원칙: LLM 백엔드는 OpenAI 호환 규격으로 추상화.** 설정 한 줄로 Ollama ↔ 클라우드 ↔ 렌트GPU 교체.

## 3. 원가 & 손익 구조

> 형님 순이익 = 구독료 − (LLM 토큰값 + 음성비 + 서버비)
> 음성은 브라우저 내장으로 ₩0 → **실질 변수는 LLM 하나.**

### 단계별 LLM 비용
- **개발·클로즈베타**: Ollama 로컬 → **₩0** (전기값만). 동시접속 소수 한정.
- **유료 오픈**: 클라우드 Haiku 4.5 + 프롬프트 캐싱 → 헤비 유저 1명 월 **약 ₩3,700** (캐싱 필수, 없으면 ~₩8,500).
- **대규모**: 렌트 GPU 자체호스팅 → 부하 꾸준할 때 토큰값보다 저렴.

### 손익을 만드는 3대 레버
1. **모델**: 스토리텔링은 Haiku급으로 충분 (Opus는 5배 비쌈)
2. **프롬프트 캐싱**: 세계관·말투·설정을 고정 프리픽스로 → 매턴 90% 절감 (RPG는 문맥 반복이 커서 효과 극대)
3. **티어별 일일 턴 캡**: 헤비 유저 적자 방지 + 무료→유료 전환 장치

## 4. 구독 티어(초안)

| 티어 | 가격 | 세계 | 하루 턴 | 목소리 |
|---|---|---|---|---|
| 무료 | ₩0 | 1~2개 | 30턴 | 기본 |
| 스탠다드 | ₩4,900/월 | 전체 | 300턴 | 기본 |
| 프리미엄 | ₩9,900/월 | 전체 + 신작 우선 | 무제한(공정사용) | 프리미엄 TTS(후속) |

> 결제: PWA라 웹 결제(토스페이먼츠/Stripe/Lemon Squeezy). 앱스토어 수수료 회피 가능. (추후 결정)

## 5. 시나리오 엔진 설계

### 게임마스터(GM) 프롬프트 = 이 게임의 심장
공통 GM 프레임워크 + 세계별 플레이버로 구성.

**공통 GM 규칙**
- 2인칭 시점("너는…")으로 장면을 생생하되 **짧게**(3~5문장, TTS로 읽히므로 구어체·짧은 문장) 묘사.
- 매 응답은 **플레이어의 선택/행동을 유도**하며 끝냄.
- 플레이어의 대사·행동을 **대신 결정하지 않음**. 말한 행동은 무엇이든 수용.
- 기존에 확정된 사실(이름/관계/소지품)과 **일관성** 유지.
- HP/소지품 등 상태는 서사적으로 추적, 긴장 점증.
- 한국어, 낭독 친화적.

**세계 팩 구조** (`worlds/*.json`)
```json
{ "id": "murim", "name": "무림: 신입 무사", "tagline": "...",
  "free": true, "opening": "첫 장면 내레이션", "system": "세계 규칙+톤+시작상황" }
```

### 상태/문맥 관리
- 대화 히스토리를 매 요청에 전송(스테이트리스). 길어지면 요약 압축(후속: 서버측 compaction).
- 고정 프리픽스(시스템 프롬프트) → 캐시 대상. 가변부(최근 대화)만 뒤에.

## 6. 아키텍처

```
[브라우저 PWA]                         [Node 백엔드]                 [LLM]
 세계선택 화면        ── /api/worlds ──▶  worlds/*.json 로드
 채팅 화면
  🎤 STT(Web Speech) ─ 사용자 발화
  💬 메시지 리스트    ── /api/chat  ──▶  system+history 조립          Ollama /v1
  🔊 TTS(SpeechSynth)◀─ GM 응답 낭독      턴 캡 체크 · 캐싱 훅   ◀──▶  (또는 클라우드)
```

- **프런트**(`web/`): Vite + TypeScript PWA. 음성 입출력, 화면, 상태.
- **백엔드**(`server/`): Express. LLM 프록시(키 은닉), 세계 팩, 턴 캡, 캐싱 위치.
- **세계 팩**(`worlds/`): JSON. 서버가 로드.

## 7. MVP 범위 (지금 만드는 것)

- [x] 세계 선택 화면 (3개 세계: 무협/이세계/좀비)
- [x] 음성 입력(말하기) → AI GM 응답 → 음성 낭독
- [x] 텍스트 입력 폴백(마이크 없거나 조용한 곳)
- [x] Ollama 백엔드 연동 + LLM 미설치 시 목(mock) 응답으로 UI 데모 가능
- [x] 간이 일일 턴 캡(로컬 clientId 기준)
- [x] PWA(설치 가능, 매니페스트/서비스워커)

## 8. 로드맵

1. **MVP 슬라이스** ← 지금
2. 스트리밍 응답(생성되는 대로 낭독) · TTS 문장 청킹
3. 세이브/로드(로컬 저장), 세계 팩 추가
4. 클라우드 백엔드 스위치 + 프롬프트 캐싱 실측
5. 로그인 + 구독 결제(웹) + 서버측 턴 캡/요약
6. 프리미엄 TTS 옵션, 세계 팩 스토어

## 9. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| 브라우저 TTS 목소리 품질(로봇틱) | 기본은 무료, 프리미엄 TTS는 유료 옵션으로 후속 |
| iOS Safari의 Web Speech STT 제약 | 텍스트 입력 폴백 상시 제공, 지원 브라우저 안내 |
| 헤비 유저 LLM 적자 | 일일 턴 캡 + 캐싱 + Haiku |
| 로컬 백엔드 확장 한계 | 백엔드 스위처블 설계로 클라우드 전환 |
| 제3자 상표·부적절 생성 | 세계 팩은 일반명, GM 프롬프트에 안전 가이드 |
