# 보이스토리 원스토어 출시 가이드 (TWA · 무료 먼저)

방향: **A안 — PWA를 TWA로 감싸 원스토어에 무료로 등록.** 음성(Web Speech)은 TWA의 크롬 엔진에서 그대로 동작. 구독은 반응 검증 후 도입.

---

## 0. 큰 그림

```
[웹 배포]                       [TWA 래핑]                 [원스토어]
프런트(정적) + 백엔드(Node)  →  Bubblewrap → AAB/APK  →  개발자센터 업로드
공개 HTTPS 도메인                assetlinks.json로 도메인 검증      등급분류·심사
```

TWA는 **웹 껍데기**라, 앱은 형님의 **공개 HTTPS 주소**를 띄웁니다. 그래서 **웹 배포가 선행**되어야 하고, 그 도메인에 `assetlinks.json`이 있어야 주소창 없이 앱처럼 실행돼요.

---

## 1. 선행 조건 — 웹 배포

### 1-1. 백엔드(Node) 배포 + LLM = Groq (이미 구성됨)
- 공개 앱은 집 Ollama로 못 버팁니다(동시접속·24h 가동). LLM은 **Groq**로 확정 (무료·초고속·OpenAI 호환).
- `server/.env` 현재값: `LLM_BASE_URL=https://api.groq.com/openai/v1`, `LLM_MODEL=qwen/qwen3.6-27b`, `LLM_API_KEY=gsk_...`.
  - ⚠️ **`.env`는 git에 커밋 금지**(.gitignore 처리됨). 배포 시엔 호스팅의 **환경변수**로 키를 넣으세요.
  - 추론모델이라 `reasoning_effort:'none'` + JSON 구조화출력 이미 적용됨(코드에 포함).
- 백엔드 호스팅: Render/Railway/Fly/VPS 등 Node 실행 가능한 곳. `PORT` 열고 HTTPS. 프런트의 `/api`를 이 백엔드로 프록시.

### 1-2. 프런트(정적) 배포
```bash
cd web && npm run build      # → web/dist
```
- `web/dist`를 정적 호스팅(Cloudflare Pages/Netlify/Vercel/HF Space 등)에 올림. **HTTPS 필수.**
- `/api` 는 백엔드로 연결(리버스 프록시) 하거나, 별도 API 도메인 사용(백엔드 CORS 이미 허용). 별도 도메인이면 프런트 코드의 `/api` 호출을 API 절대주소로 바꾸거나 프록시 설정.

### 1-3. 도메인 확정
- 예: `https://voicestory.example.com` (이 도메인이 TWA가 여는 주소이자 assetlinks 위치).

---

## 2. PWA가 TWA 조건을 만족하는지 확인
- [x] `manifest.webmanifest` (name/short_name/start_url/display=standalone/theme/background/**PNG 아이콘 512**)
- [x] 서비스워커 등록 (`sw.js`)
- [x] HTTPS 배포
- 크롬 DevTools > Lighthouse(PWA) 통과 확인 권장.

---

## 3. TWA 래핑 (Bubblewrap)

```bash
npm i -g @bubblewrap/cli

# 배포된 manifest URL로 초기화
bubblewrap init --manifest https://voicestory.example.com/manifest.webmanifest
#  - Application ID: com.limoon.voicestory  (원스토어에서 고유해야 함)
#  - App name / Launcher name: 보이스토리
#  - Display mode: standalone
#  - 서명 키 생성(안내대로) → 비밀번호·별칭 기억! (분실 시 업데이트 불가)

bubblewrap build
#  → app-release-signed.apk  및  app-release-bundle.aab 생성
#  → 서명 키의 SHA256 지문 출력됨 (다음 단계에서 사용)
```

> 참고: `android/twa-manifest.json` 에 미리 채운 템플릿을 뒀어요. 도메인·패키지명만 바꿔 재사용 가능.

---

## 4. 도메인 검증 — assetlinks.json

`bubblewrap`이 출력한 **SHA256 지문**을 `web/public/.well-known/assetlinks.json` 의 `sha256_cert_fingerprints` 에 넣고 재배포.
최종적으로 아래 주소로 접근돼야 함:
```
https://voicestory.example.com/.well-known/assetlinks.json
```
(템플릿은 이미 `web/public/.well-known/assetlinks.json` 에 있음 — 패키지명·지문만 교체)

검증 실패하면 앱 상단에 주소창이 뜹니다(기능은 됨). 지문·경로 확인.

---

## 5. 원스토어 등록

1. **원스토어 개발자센터** 가입/로그인 (사업자: 개인사업자 등록 완료 상태).
2. 새 앱 등록 → **AAB 또는 APK 업로드** (원스토어는 둘 다 지원).
3. 스토어 정보 입력: 앱명, 설명, **아이콘(512)**, 스크린샷, 카테고리(게임).
4. **등급분류**: 국내 게임은 등급분류 필요. 원스토어 등록 과정의 자체등급분류 절차를 따름 — AI 생성 콘텐츠 관련 문항 정확히 응답. (상세는 개발자센터 최신 안내 확인)
5. 심사 제출 → 승인 후 출시.

---

## 6. 이번 단계에서 뺀 것 (다음 마일스톤)

- **구독 결제**: 원스토어 IAP는 네이티브 Java SDK라 순수 TWA로는 호출 불가. 정식 구독은 **B안(네이티브 셸 + 원스토어 IAP + 네이티브 STT/TTS)** 에서. 그전까지는 무료 또는 웹결제.
- **스트리밍 응답**(생성되는 대로 낭독)은 아직 미구현 — 체감속도 개선용 다음 과제.
- (완료: webp 경량화·세계별 의상 8표정·세이브/이어하기·6세계 방향성)

---

## 체크리스트

- [ ] 백엔드 배포(Node) + Groq 키를 호스팅 **환경변수**로 (`.env` 커밋 금지)
- [ ] `npm run build` → 정적 프런트 배포(HTTPS)
- [ ] 도메인 확정
- [ ] manifest PNG 아이콘 512 포함 확인
- [ ] `bubblewrap init` → `build` → SHA256 확보
- [ ] `assetlinks.json`에 지문 넣고 `/.well-known/`에 배포
- [ ] 원스토어 개발자센터: AAB 업로드 + 아이콘/스크린샷 + 등급분류 + 심사
